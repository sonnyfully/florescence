# Character FX

Florescence by Solace: a stereo character processor plugin for the Kissland / After Hours palette.

This repo is currently in **Stage 0 — Pre-flight**. Stage 0 is documentation and setup only; no plugin code or bundle metadata should be added until the name, vendor, and bundle identity stop points are resolved.

## Current Status

- Canonical docs live in `docs/`.
- Open stop points live in `docs/open_questions.md`.
- Stage status lives in `docs/roadmap.md`.
- Bundle ID planned for Stage 1: `com.solace.florescence`.
- Private beta-customer notes should use local `docs/customers.md`, which is ignored by git.

## Build

No build target exists yet. Per the roadmap, CMake/JUCE plumbing starts in Stage 1 after Stage 0 is complete.

## Agent Entry Point

Read `AGENTS.md` first, then follow the read order listed there.
